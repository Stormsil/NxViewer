<Window x:Class="NxTiler.HotkeysWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка горячих клавиш" Width="400" SizeToContent="Height"
        Background="{StaticResource ProxmoxDarkGray}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="120"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Overlay -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Показать/Скрыть Оверлей:" Margin="0,0,0,12"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="BoxOverlay" PreviewKeyDown="Box_PreviewKeyDown" Tag="Overlay" Margin="0,0,0,12"/>

        <!-- Main Window -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Показать/Скрыть Главное окно:" Margin="0,0,0,12"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="BoxMain" PreviewKeyDown="Box_PreviewKeyDown" Tag="Main" Margin="0,0,0,12"/>

        <!-- Focus Mode -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Режим Фокуса:" Margin="0,0,0,12"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="BoxFocus" PreviewKeyDown="Box_PreviewKeyDown" Tag="Focus" Margin="0,0,0,12"/>

        <!-- Minimize -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Свернуть/Развернуть все:" Margin="0,0,0,12"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="BoxMinimize" PreviewKeyDown="Box_PreviewKeyDown" Tag="Minimize" Margin="0,0,0,12"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Сброс" Click="BtnReset_Click" Style="{StaticResource ProxmoxButton}" Margin="0,0,8,0"/>
            <Button Content="Сохранить" Click="BtnSave_Click" Style="{StaticResource ProxmoxAccentButton}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
